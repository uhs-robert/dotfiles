# Oasis workstation identity
# Single source of truth for paths that should resolve to the primary user's
# home even when a tool is running as root.
PRIMARY_USER="roberth"
PRIMARY_HOME="/home/$PRIMARY_USER"
