# Installed by install_nvidia after its prompt, never by the main package pass.
[USERSPACE]
nvidia-utils
libva-nvidia-driver
nvidia-settings
nvidia-prime

# One of these two, chosen at the prompt.
[DKMS]
nvidia-dkms
linux-headers

[MODULE]
nvidia
