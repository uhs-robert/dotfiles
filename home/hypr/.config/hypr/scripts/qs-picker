#!/usr/bin/env bash
# hypr/.config/hypr/scripts/qs-picker
# Opens a Quickshell picker, or runs the fallback command when no running bar has that picker.
provider="$1"
fallback="$2"
[ "$("$(dirname "$0")/qs-ipc" call picker open "$provider" 2>/dev/null)" = ok ] || exec sh -c "$fallback"
